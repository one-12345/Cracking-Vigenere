#include <iostream>
#include <vector>
#include <cstdio>
using namespace std;
int main(){
    string filename;
    cin >> filename;
    int n;
    cin >> n;
    vector<string> s(n);
    vector<string> enc(n);
    freopen(filename.c_str(),"r",stdin);
    string str;
    while(cin >> str){
        
    }
}